{
  "name": "crypte_u_data",
  "version": "1.0.0",
  "description": "Crypte and decript your data",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon ./src/server.ts",
    "prettier": "prettier --write \"**/*.ts\""
  },
  "dependencies": {
    "crypto": "^1.0.1"
  }
}
